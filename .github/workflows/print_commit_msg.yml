name: Print Comiit Message
on:
  push:
    branches:
      - 'main'
jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
        - name: checkout code
          uses: actions/checkout@v4
        - name: Print Message
          run: |
            echo "Commit Message:"
            git log -l --pretty=%B